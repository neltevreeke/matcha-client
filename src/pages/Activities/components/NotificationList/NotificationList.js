import React from 'react'
import styles from './NotificationList.scss'

const NotificationList = () => {
  return (
    <div className={styles.component}>
      <p className={styles.title}>Received notifications</p>
    </div>
  )
}

export default NotificationList
